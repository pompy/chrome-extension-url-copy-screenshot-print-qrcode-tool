// content.js//somdutt ganguly//tosomdutt@gmail.comchrome.runtime.onMessage.addListener(  function(request, sender, sendResponse) {	    if( request.message === "clicked_browser_action" ) {		  var url = window.location.href;    var temp = document.createElement("textarea");  temp.textContent = url;  document.body.appendChild(temp);  temp.select();  document.execCommand('copy');  temp.blur();  document.body.removeChild(temp);    chrome.runtime.sendMessage({"message": "url_fetched", "data": url});	}		 if( request.message === "print" ) {		 window.print();	 }     if( request.message === "bookmark" ) {		 alert("Bookmark created successfully. Please check bookmark manager to find bookmarks");	 }  });			/*//clipboard api having some issues			navigator.permissions.query({name: "clipboard-write"}).then(result => {  if (result.state == "granted" || result.state == "prompt") {    navigator.clipboard.writeText(url)  .then(() => {    console.log('Text copied to clipboard');  })  .catch(err => {    alert('Could not copy text: ' + err);  });  }});*/